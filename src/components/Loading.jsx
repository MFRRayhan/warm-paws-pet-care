const Loading = () => {
  return (
    <div className="flex items-center justify-center h-screen">
      <span className="loading loading-bars loading-xl text-[#FF6B6B]"></span>
    </div>
  );
};

export default Loading;
